# Local development (API / Prisma / tests against Postgres from docker compose)
DATABASE_URL=postgresql://skillsphere:skillsphere@localhost:5432/skillsphere?schema=public
PORT=4000
INDEXER_INTERVAL_MS=5000
